/*
 * Create the function factorial here
 */
var fac=1;
function factorial(n){
    for(let i=1;i<=n;i++){
        
        fac=fac*i;
    }
    return fac;
}

/*
 * Create the function factorial here(Approach-2)
 */
function factorial(n){
    return n ? n * factorial(n - 1) : 1;
}
